%h1 Listing messages

%table
  %tr
    %th Title
    %th User
    %th Reply count
    %th Last post date
    %th

  - @messages.each do |message|
    - present message do |message_presenter|
      %tr
        %td= link_to message.title, message
        %td= link_to message.user.username, user_path(message.user)
        %td= message.reply_count
        %td= message.date
        %td
          = message_presenter.edit_link
          = message_presenter.destroy_link

%br

= link_to 'New Message', new_user_message_path(current_user)
