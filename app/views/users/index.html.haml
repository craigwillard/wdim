%h1 Listing Users

%table
  %tr
    %th Username
    %th First name
    %th Last name
    %th Email
    %th User type
    %th Member since
    %th Posts count
    %th At work
    %th Active
    %th Options

  - @users.each do |user|
    %tr
      %td= user.username
      %td= user.first_name
      %td= user.last_name
      %td= user.email
      %td= user.user_type
      %td= user.member_since
      %td= user.posts_count
      %td= user.at_work
      %td= user.active
      %td
        = link_to 'Avatars', user_avatars_path(user)
        = link_to 'Show', user
        = link_to 'Edit', edit_user_path(user)
        = link_to 'Destroy', user, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New User', new_user_path
